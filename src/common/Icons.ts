let imgPath = '../assets/images/';

const Icons = {
  setting: require(`${imgPath}setting.png`),
  back: require(`${imgPath}back.png`),
  sync: require(`${imgPath}sync.png`),
  star: require(`${imgPath}star.png`),
};

export default Icons;